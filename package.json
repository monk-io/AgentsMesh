{
  "name": "agentsmesh-workspace-root",
  "private": true,
  "description": "Workspace root for Bazel aspect_rules_js — pnpm workspace packages live under clients/ and packages/.",
  "devDependencies": {
    "@playwright/test": "1.50.1"
  },
  "pnpm": {
    "overrides": {
      "@playwright/test": "1.50.1",
      "playwright": "1.50.1",
      "playwright-core": "1.50.1"
    }
  }
}
