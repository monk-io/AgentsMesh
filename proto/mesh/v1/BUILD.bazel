load("@rules_go//proto:def.bzl", "go_proto_library")
load("@rules_proto//proto:defs.bzl", "proto_library")
load("//tools/validate_prost_tags:rule.bzl", "validate_prost_tags")

# Bazel-native protobuf code generation for mesh.v1.
#
# Mirrors proto/binding/v1/BUILD.bazel — same hermetic protoc/protoc-gen-go
# pipeline, same `proto/gen/go/<svc>/v1` importpath shape. The Rust prost
# DTOs are hand-maintained at clients/core/crates/types/src/mesh_proto.rs;
# `tools/validate_prost_tags` pins .proto field number ↔ Rust
# `prost(tag = N)` mapping at build time (watch list hazard #8).
#
# TS message classes for the binary wire (conventions §2.5) ship as a
# committed mirror under proto/gen/ts/mesh/v1/ — produced by
# `pnpm proto:gen-ts`.

proto_library(
    name = "mesh_proto",
    srcs = ["mesh.proto"],
    visibility = ["//visibility:public"],
)

go_proto_library(
    name = "mesh_go_proto",
    compilers = ["@rules_go//proto:go_proto"],
    importpath = "github.com/anthropics/agentsmesh/proto/gen/go/mesh/v1",
    proto = ":mesh_proto",
    visibility = ["//visibility:public"],
)

# Watch list hazard #8 — catch hand-written prost struct drift.
validate_prost_tags(
    name = "mesh_validate",
    proto = "mesh.proto",
    rust = "//clients/core/crates/types:src/mesh_proto.rs",
)
