name: Bazel

# Unified CI workflow — one entry point for everything. Delegates to
# Bazel targets where they exist (Go tests, Rust tests, buildifier,
# Vitest) and falls back to pnpm / go / docker CLIs for jobs whose
# Bazel equivalents aren't wired yet (lint, Next.js build, Docker
# image verify, Playwright e2e, iOS XCFramework).
#
# Legacy ci.yml / e2e.yml / ios.yml are deleted; this file is the only
# PR gate. release.yml (tag-triggered) is kept separate — it's the
# deploy pipeline, not a CI gate.

on:
  push:
    branches: [main, develop]
  pull_request:
  workflow_dispatch:

concurrency:
  group: bazel-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  GO_VERSION: "1.25"
  NODE_VERSION: "20"
  PNPM_VERSION: "9"

jobs:
  # ===========================================================================
  # Bazel-native: lives inside the sandbox, uses Bazel remote cache.
  # ===========================================================================

  smoke:
    name: Smoke (info + buildifier)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: bazel-contrib/setup-bazel@0.9.1
        with:
          bazelisk-cache: true
          disk-cache: true
          repository-cache: true
      - run: bazel info workspace
      - run: bazel run //:buildifier_check

  go:
    name: Go (tests)
    runs-on: ubuntu-latest
    timeout-minutes: 45
    needs: smoke
    steps:
      - uses: actions/checkout@v4
      - uses: bazel-contrib/setup-bazel@0.9.1
        with:
          bazelisk-cache: true
          disk-cache: true
          repository-cache: true
      # Full Go module graph. CGO is enabled for SQLite-backed testkit
      # integration tests. Release builds flip to pure Go via
      # --config=release.
      # --flaky_test_attempts=3: TestPtyReaderForwardsOutput is a
      # PTY/TCP-timing test that occasionally hits i/o timeouts under
      # CI load. The timeout is 2s; local runs are consistently green.
      - run: bazel test --flaky_test_attempts=3 //backend/... //runner/... //relay/... //agentfile/... //proto/...

  rust:
    name: Rust Core (clients/core)
    runs-on: ubuntu-latest
    timeout-minutes: 45
    needs: smoke
    steps:
      - uses: actions/checkout@v4
      - uses: bazel-contrib/setup-bazel@0.9.1
        with:
          bazelisk-cache: true
          disk-cache: true
          repository-cache: true
      # Every crate under clients/core/crates/... has a Bazel target;
      # 12 test targets were green on the migration branch.
      # --build_tests_only: skip building non-test library targets in
      # the same packages. The ffi xcframework target is a sh_binary
      # tagged `manual + requires-darwin`, wasm_pkg needs wasm-bindgen —
      # both get filtered out here and own their own jobs.
      - run: bazel test --build_tests_only //clients/core/crates/...

  # ===========================================================================
  # Shell fallback — the workflow drives pnpm / go / docker directly.
  # Bazel equivalents tracked in the "TODO" block below.
  # ===========================================================================

  go-lint:
    name: Go Lint (backend + runner + relay)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: smoke
    strategy:
      fail-fast: false
      matrix:
        module: [backend, runner, relay]
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}
          cache-dependency-path: ${{ matrix.module }}/go.sum
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          working-directory: ${{ matrix.module }}
          install-mode: goinstall
          version: latest
          args: --timeout=5m

  go-windows:
    name: Runner Tests (Windows)
    runs-on: windows-latest
    timeout-minutes: 20
    needs: smoke
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}
          cache-dependency-path: runner/go.sum
      - name: Download dependencies
        working-directory: runner
        run: go mod download
      - name: Run tests
        working-directory: runner
        shell: bash
        # Retry the flaky PTY/TCP-timing test (TestPtyReaderForwardsOutput)
        # up to 3 times. `go test -count` doesn't retry-only-on-fail, so
        # use a bash loop that re-runs the specific failing test on first
        # pass failure.
        run: |
          set +e
          go test -race -coverprofile=coverage.out ./...
          rc=$?
          if [ $rc -ne 0 ]; then
            echo "::warning::Re-running runner tests (attempt 2/3)"
            go test -race ./internal/poddaemon/...
            rc=$?
          fi
          if [ $rc -ne 0 ]; then
            echo "::warning::Re-running runner tests (attempt 3/3)"
            go test -race ./internal/poddaemon/...
            rc=$?
          fi
          exit $rc
      - name: Upload coverage
        uses: codecov/codecov-action@v5
        with:
          files: runner/coverage.out
          flags: runner-windows
          fail_ci_if_error: false

  web:
    name: Web (lint + type-check + vitest)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: smoke
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v5
        with:
          version: ${{ env.PNPM_VERSION }}
      - uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Lint
        working-directory: clients/web
        run: pnpm lint
      - name: Type check
        working-directory: clients/web
        run: pnpm type-check
      - name: Vitest
        working-directory: clients/web
        run: pnpm test:run

  web-admin:
    name: Web-Admin (lint + build)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: smoke
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v5
        with:
          version: ${{ env.PNPM_VERSION }}
      - uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Lint
        working-directory: clients/web-admin
        run: pnpm lint
      - name: Build
        working-directory: clients/web-admin
        run: pnpm build

  docker-verify:
    name: OCI Image Build (${{ matrix.service }})
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: smoke
    if: github.event_name == 'pull_request'
    strategy:
      fail-fast: false
      matrix:
        include:
          - service: backend
            target: //backend/cmd/server:image
          - service: runner
            target: //runner/cmd/runner:image
          - service: relay
            target: //relay/cmd/relay:image
    steps:
      - uses: actions/checkout@v4
      - uses: bazel-contrib/setup-bazel@0.9.1
        with:
          bazelisk-cache: true
          disk-cache: true
          repository-cache: true
      # Replaces the legacy ci/<service>.Dockerfile verify. The rules_oci
      # target produces the same distroless-based image release.yml pushes
      # to Docker Hub + registry.agentsmesh.ai on main.
      - run: bazel build ${{ matrix.target }}

  ios-xcframework:
    name: Build AgentsMeshCore.xcframework
    runs-on: macos-14  # arm64 runner with Xcode 15+
    timeout-minutes: 30
    needs: smoke
    steps:
      - uses: actions/checkout@v4
      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_16.app || sudo xcode-select -s /Applications/Xcode.app
      - uses: bazel-contrib/setup-bazel@0.9.1
        with:
          bazelisk-cache: true
          disk-cache: true
          repository-cache: true
      # Pure-Bazel XCFramework pipeline: rules_rust supplies the iOS
      # triples (extra_target_triples in MODULE.bazel), our
      # rust_xcframework macro (build_defs/ios) transitions the
      # rust_static_library through three iOS platforms, lipos the
      # simulator pair, runs uniffi-bindgen, and assembles the tree
      # artifact. No cargo/rustup setup needed — Bazel handles it.
      - name: Build XCFramework
        run: bazel build //clients/core/crates/ffi:AgentsMeshCore //clients/core/crates/ffi:AgentsMeshCore_swift
      - name: Inspect output
        run: |
          ls -la bazel-bin/clients/core/crates/ffi/AgentsMeshCore.xcframework/
          wc -l bazel-bin/clients/core/crates/ffi/AgentsMeshCore_bindings_out/AgentsMeshCore.swift
      - name: Package artifact
        run: |
          # Stage the bazel-built xcframework next to the Swift glue
          # (both live under bazel-bin/). Use ditto to preserve the
          # read-only tree artifact permissions; `zip` chokes on them.
          mkdir -p dist
          ditto bazel-bin/clients/core/crates/ffi/AgentsMeshCore.xcframework \
                dist/AgentsMeshCore.xcframework
          cp bazel-bin/clients/core/crates/ffi/AgentsMeshCore_bindings_out/AgentsMeshCore.swift \
             dist/AgentsMeshCore.swift
          cd dist && zip -r AgentsMeshCore-xcframework.zip \
                          AgentsMeshCore.xcframework AgentsMeshCore.swift
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: AgentsMeshCore-xcframework
          path: dist/AgentsMeshCore-xcframework.zip
          retention-days: 14

  # =========================================================================
  # TODO: Bazel-native replacements tracked below. Each jobs above
  # currently uses shell/CLI because the Bazel target isn't ready:
  # =========================================================================

  # next-build:
  #   Blocked on: next_app() label format for `next_js_binary`.
  #   Re-enable with `bazel build //clients/web //clients/web-admin`.

  # e2e-playwright:
  #   Playwright e2e targets (//clients/web:e2e,
  #   //clients/web/e2e-playwright:e2e, //clients/desktop:e2e) are
  #   green locally but need docker dev-stack setup in CI before
  #   enabling.

  # oci-images:
  #   rules_oci push targets //{backend,runner,relay}/cmd/*:image_push
  #   exist; release.yml will wire them once dual-registry + semver
  #   tag generation is ported (currently docker/metadata-action does
  #   that).
