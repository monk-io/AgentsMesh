[package]
name = "uniffi-bindgen"
description = "Bindings generator CLI for agentsmesh-ffi (Swift/Kotlin)"
version.workspace = true
edition.workspace = true

[[bin]]
name = "uniffi-bindgen"
path = "src/main.rs"

[dependencies]
uniffi = { workspace = true, features = ["cli"] }
